import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
const api = readFileSync("src/routes/api/watch-party.ts", "utf8");
const ui = readFileSync("src/components/player/WatchPartyRoomPanel.tsx", "utf8");
assert.match(api, /set-password/);
assert.match(api, /password_hash=\$3 where id=\$1 and host_id=\$2/);
assert.match(api, /memberOrStaff\(user, partyId\)/);
assert.match(ui, /void syncPlayback\(\)/);
assert.match(ui, /syncPlayback\(true\)/);
assert.match(ui, /type="password"/);
assert.match(api, /raw_app_meta_data->>'role'='vip'/);
assert.match(api, /raw_app_meta_data->>'role'='admin'/);
assert.match(ui, /watch-party-message-vip/);
assert.match(ui, /message\.staff_role === "vip" \? "VIP"/);
console.log("PASS static guards: host password permission, member playback, role-aware chat, auto/manual sync");
